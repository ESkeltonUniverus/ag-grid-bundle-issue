{
  "name": "ag-grid-bundle-issue-monorepo",
  "private": true,
  "packageManager": "pnpm@10.6.5",
  "scripts": {
    "build:combined": "pnpm --filter ag-grid-bundle-issue build && pnpm --filter ag-grid-bundle-issue build:source",
    "build": "pnpm --filter ag-grid-bundle-issue build",
    "build:local": "pnpm --filter ag-grid-bundle-issue build:source"
  }
}
